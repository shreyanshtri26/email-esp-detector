# server/.env (copy this file there and fill in values)

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/email-esp-detector

# IMAP Configuration
IMAP_HOST=your-imap-server.com
IMAP_PORT=993
IMAP_SECURE=true
IMAP_USER=your-email@example.com
IMAP_PASS=your-email-password
IMAP_MAILBOX=INBOX

# Server Configuration
PORT=5000
NODE_ENV=production
